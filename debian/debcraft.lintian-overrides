# These shell files are not intended to be executed as-is, but use the .inc.sh
# suffix to show that they are includes of shell scripts
debcraft: script-not-executable [usr/share/debcraft/*.inc.sh]

# README in improve.d describes the directory's purpose and scripts
debcraft: package-contains-documentation-outside-usr-share-doc [usr/share/debcraft/container/improve.d/README.md]
